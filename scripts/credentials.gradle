file("../credentials.properties").with { propFile ->
    if (propFile.canRead()) {
        def properties = new Properties()
        properties.load(new FileInputStream(propFile))

        ext.SOZDIK_API_CLIENT_NAME = properties['sozdikApiClientName']
        ext.SOZDIK_API_KEY = properties['sozdikApiKey']
    } else {
        println 'Unable to read credential.properties, using defaults'
        ext.SOZDIK_API_CLIENT_NAME = "test"
        ext.SOZDIK_API_KEY = "test"
    }
}